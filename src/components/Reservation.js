import Navbar from './Navbar';

const Reservation = () => (
  <div className="App">
    <Navbar />
    <main className="main">
      <h1>This is the Reservations page</h1>
    </main>
  </div>
);

export default Reservation;
